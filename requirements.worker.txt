# Torch CPU wheels (smaller than full CUDA builds)
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

redis
sqlalchemy
psycopg2-binary
pydantic-settings
alembic
langchain

# Parsing + embeddings
unstructured[local-inference]
sentence-transformers

# Only worker needs this (not API)
python-magic
celery[redis]
chromadb-client

